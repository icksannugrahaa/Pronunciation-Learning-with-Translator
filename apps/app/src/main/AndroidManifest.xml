<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ninepm.english.learn">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_logo_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.EnglishLearn"
        tools:targetApi="m">
        <activity android:name=".ui.question.QuestionsActivity" />
        <activity android:name=".ui.registration.RegistrationActivity"
            android:theme="@style/Theme.EnglishLearn.NoActionBar"/>
        <activity android:name=".ui.score.ScoreActivity" />
        <activity android:name=".ui.changepassword.ChangePasswordActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.history.HistoryActivity" />
        <activity android:name=".ui.about.AboutActivity" />
        <activity android:name=".ui.verification.VerificationSuccessActivity" />
        <activity android:name=".ui.verification.VerificationWaitActivity" />
        <activity
            android:name=".ui.question.BasicQuestionActivity"
            android:theme="@style/Theme.EnglishLearn.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/Theme.EnglishLearn.NoActionBar" />
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/Theme.EnglishLearn.NoActionBar" />
        <activity
            android:name=".ui.splashscreen.SplashScreenActivity"
            android:theme="@style/Theme.EnglishLearn.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>